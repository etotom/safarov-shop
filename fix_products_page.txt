# Replace the interface at the top
interface ProductsPageProps {
  searchParams: {
    search?: string  # ADD THIS LINE
    category?: string
    minPrice?: string
    maxPrice?: string
    sortBy?: string
    page?: string
  }
}
